# -*- mode: ruby -*-
# vi: set ft=ruby :

# This really only exists to test the new tasks with

Vagrant.configure("2") do |config|

  config.vm.define "one" do |one|
    one.vm.box = "ubuntu/bionic64"
  end

  config.vm.define "two" do |two|
    two.vm.box = "ubuntu/bionic64"
  end

end
